import Navigation from './Navigation';

const Header = () => {
  return (
    <header className="border-primary-100 flex border-b px-8 py-4">
      <Navigation />
    </header>
  );
};

export default Header;
