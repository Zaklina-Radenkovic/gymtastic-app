import Navigation from './Navigation';

const Header = () => {
  return (
    <header className="border-primary-300 bg-primary-100 flex justify-end border-b px-8 py-4">
      <Navigation />
    </header>
  );
};

export default Header;
